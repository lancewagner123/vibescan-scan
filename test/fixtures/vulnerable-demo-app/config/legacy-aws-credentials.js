'use strict';

// The AWS credentials that used to live in this file were removed here as part of a
// (fictional, fixture-only) incident response -- but they remain fully visible in this
// repo's git history via `git log -p` on this file, which is exactly what check 3
// (secret-git-history) is designed to catch: a secret found only in history, not HEAD.
// See ../README.md for the exact commit hashes.
module.exports = {};
