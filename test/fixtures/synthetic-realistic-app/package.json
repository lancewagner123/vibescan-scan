{
  "name": "billsy-api",
  "version": "0.4.2",
  "private": true,
  "description": "VibeScan test fixture: a moderately realistic small Express + Postgres + Stripe SaaS backend, seeded with a natural MIX of real vulnerabilities across different checks landing in the same handlers/files -- the way an actual bad app would ship them, not one-liner isolation. See README.md in this folder for exactly what was seeded where.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "cookie-parser": "^1.4.6",
    "pg": "^8.11.0",
    "stripe": "^14.0.0",
    "lodash": "4.17.15"
  }
}
