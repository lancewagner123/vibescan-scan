# Placeholder template - NOT a real secret file. Values are recognized placeholders
# (changeme/example) so ONLY check 2 (filename) fires, isolating the check-2 false positive.
API_KEY=changeme
DATABASE_URL=changeme
STRIPE_SECRET=changeme
