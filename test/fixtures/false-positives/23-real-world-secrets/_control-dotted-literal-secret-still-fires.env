// Positive control -- NOT a false positive. Overcorrection guard for the round-4
// adversarial fix to CODE_REFERENCE_ROOT_RE/looksLikeCodeReference: a genuine high-entropy
// secret assigned via the UNQUOTED dotenv-style path (GENERIC_ENTROPY_UNQUOTED_RE) that
// happens to contain literal dots and no other non-identifier characters must still be
// flagged as secret-hardcoded-generic. Before the fix, CODE_REFERENCE_VALUE_RE matched ANY
// dot-separated identifier-charset value regardless of whether it actually started with a
// known-safe env-access root (process.env./import.meta.env./etc.), so this exact shape was
// silently dropped with zero findings. Prefixed with an underscore so it sorts away from
// the actual false-positive fixtures and is unmistakably a control.
DB_ADMIN_SECRET=xK2mQ9pL4vR8tY1wZ3aB5cD7eF.gH0iJ6kL2mN4oP8qR1sT3uV5wX7yZ9a.bC1dE3fG5hI7jK9lM1nO3pQ5rS7tU9vW
