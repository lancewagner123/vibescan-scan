# Positive control -- NOT a false positive. Round-6 adversarial fix (2026-07-25):
# looksLikeCodeReference's per-segment shape check (digit density <=15%, case transitions
# <=3) was a real signal but not a rare enough one on its own -- an independent re-
# verification's Monte Carlo over 200,000 realistic random base62 dot-chain secrets found
# ~1.06% pass that shape check purely by chance (this isn't a crafted worst case, it's an
# ordinary random draw -- the exact SendGrid/HMAC "id.sig" token shape, not the maximally
# digit-dense/case-chaotic value the older `_control-dotted-jwt-secret-still-fires.env`
# fixture uses). These three are reproductions taken directly from that re-verification.
# looksLikeCodeReference now also requires each segment to decompose into a small number of
# pronounceable identifier-shaped "words" (hasIdentifierWordShape) -- none of these do, so
# they must still fire.

API_SECRET_ONE=vgbMLqt7Kyi.GopxURSG1
API_SECRET_TWO=st6ZPuoebJj.fvrWD1sJq
API_SECRET_THREE=A9OWETPC.RFpwTs5TTXEI
